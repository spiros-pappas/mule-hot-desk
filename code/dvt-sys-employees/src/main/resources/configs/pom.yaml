# Do not edit this file
# values will be replaced by Maven

application:
  groupId: "${project.groupId}"
  artifactId: "${project.artifactId}"
  name: "${project.name}"
  version: "${project.version}"
  versionId: "${api.versionId}"
  domain: "${api.domain}"
  runtime: "${app.runtime}"